import { PageContainer } from '../components/common/PageContainer';

export const SettingsPage = () => (
  <PageContainer
    title="Settings"
    description="Control workspace preferences, profile details, and system behavior."
  >
    <p>Settings sections, toggles, and access controls can be integrated here.</p>
  </PageContainer>
);
